[build]
builder = "NIXPACKS"
buildCommand = "cd services/etl && pip install -r ../../requirements.txt"

[deploy]
startCommand = "cd services/etl && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

